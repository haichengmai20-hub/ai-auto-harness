#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
BUN=/home/ps/dcr_claude_home/.bun/bin/bun

cd "$ROOT_DIR"
exec "$BUN" ./proxy/anthropic-qwen-proxy.ts "$@"
