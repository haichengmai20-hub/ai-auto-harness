# Fix: CC 快赢批 #2 — H3/F7/F8/F10

**日期**: 2026-06-16
**严重度**: P2（成功率/效率/可诊断性，非阻塞）
**触发**: framework-issues-cc-complete.md 快赢批剩余项（#42 CC 同步后清尾）
**关联**: [framework-issues-cc-complete.md](../../framework-issues-cc-complete.md) H3/F7/F8/F10；[framework-improvements-for-cc.md](../../framework-improvements-for-cc.md) 同号

---

## 人话版

把四个「局部、无架构」的小坑一次清掉：装包只装了 Python 壳没装系统命令(H3)、依赖一个一个装烧好几轮(F7)、子进程被内存杀手干掉却看不到错误(F8)、`.bashrc` 有语法错每条命令都先吐两行错(F10)。都是改 SKILL 指令，不碰架构。

## 四项

| # | 状态(改前) | 落点 | 改动 |
|---|---|---|---|
| **H3** apt 系统依赖 | 已修-Hermes(6d),CC 未同步 | `install-env/SKILL.md` 新增第 6.5 步 | 9 组 Python↔apt 映射(sox/pydub/librosa/soundfile/av/cv2/pytesseract/pdf2image/wand),grep repo import + `dpkg -s` 缺则 `apt-get install`,失败记 warnings 不阻塞 |
| **F7** 批量装依赖 | 待CC(净新) | `run-and-repair/SKILL.md` 第 4 步 | `grep "No module named" \| sort -u` 一次批量 `pip install`;连续 2 轮 dep_missing→深度扫描;顶层名↔pip 名映射;依赖类不计轮(R3/P11) |
| **F8** 子进程 stderr 丢失 | 待CC(净新) | `run-and-repair/SKILL.md` 第 2 步 | exit≠0 且日志无 traceback → `dmesg \| grep OOM`;有 OOM 按 CUDA_OOM 处理,无则 unknown+dmesg 尾巴入 run.json |
| **F10** .bashrc 污染 | 已间接修(heredoc),根因仍在 | `install-env/SKILL.md` 第 1 步 | `bash -n ~/.bashrc` 失败 → 本 phase `export BASH_ENV=/dev/null`(不改全局 .bashrc，R1) |

## 范式说明

CC 版是 LLM 读 SKILL.md 指令,所以 H3 这种「检测+装」给的是 bash 块(SubAgent 直接跑);F7/F8 是「遇到 X 这样做」的判断指引 + bash 模板。H3 的映射表与 Hermes `phase-install-env.sh:229` 的 6d 块逐项一致。

## 影响范围

- `.claude/skills/install-env/SKILL.md`（第 1 步 F10 + 新增第 6.5 步 H3 + ChangeLog）
- `.claude/skills/run-and-repair/SKILL.md`（第 2 步 F8 + 第 4 步 F7 + 反模式 + ChangeLog）

## 验证

- [x] H3 映射表与 Hermes 6d 逐项一致;`dpkg -s` 检测 + 装失败走 warnings 不阻塞
- [x] F7 `sort -u` 去重批量;连续 2 轮 dep_missing 触发深度扫描;依赖类不计轮与 R3/P11 一致
- [x] F8 仅在 `exit≠0 且无 traceback` 时触发 dmesg(不误扰正常失败)
- [x] F10 只 `export BASH_ENV`(本 phase),不写 ~/.bashrc(守 R1)
- [ ] 实战:下一个含系统依赖/深依赖链/OOM 的 CC cron run

## 状态

- [x] fix.md + 两 SKILL 改动 + ChangeLog
- [x] framework-issues-cc-complete.md 标 CC 已同步
- [ ] commit + 回填 hash

## 修复结果

- **commit hash**: (待回填)
